# Public and safe to expose in browser bundles.
NEXT_PUBLIC_APP_NAME=Promptube Admin

# Optional server metadata used by the healthcheck.
# Use "development" for npm run dev and "local" for the Docker stack.
APP_ENV=development
APP_VERSION=0.1.0
